pipeline {
    agent any

    stages {
        stage('Compilar') {
            steps {
                sh 'cd /home/marta/Escritorio/TFG/appp && docker-compose up --build -d'
            }
        }
    }
}

